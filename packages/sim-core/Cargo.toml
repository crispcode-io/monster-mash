[package]
name = "sim_core"
version = "0.1.0"
edition = "2021"
description = "Deterministic simulation utilities for Monster Mash."
license = "UNLICENSED"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
